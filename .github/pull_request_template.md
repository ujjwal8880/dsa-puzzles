## What does this PR do?

<!-- One paragraph. What changed and why? -->

## Type of change

- [ ] Bug fix
- [ ] New question
- [ ] New feature
- [ ] Refactor / cleanup
- [ ] Docs / config only

## Checklist

- [ ] `npm run build` passes with zero errors
- [ ] `npm run lint` passes with zero warnings
- [ ] New questions include all required fields (`title`, `slug`, `difficulty`, `category`, `patternName`, `descriptions`, `hints`, `dryRunSteps`, `complexity`, `codeSolutions`, `xpRewards`)
- [ ] No hardcoded secrets or API keys committed
- [ ] PR title follows `type: short description` format (e.g. `feat: add merge intervals question`)
- [ ] Branch name follows `type/short-description` format (e.g. `feat/merge-intervals`)

## Screenshots (if UI changed)

<!-- Drop a before/after screenshot here -->

## Related issues

<!-- Closes #123 -->
